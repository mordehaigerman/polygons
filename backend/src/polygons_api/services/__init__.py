"""Services / use-case layer.

Module-level async functions composing geometry validation and repository
operations. No ``PolygonService`` class -- controllers call these directly with
the repository injected by FastAPI.
"""
